@model Application.ViewModels.SaveSerieViewModel
@{
    ViewData["Title"] = "Eliminar serie";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>¿Está seguro que desea eliminar @Model.SerieName del listado?</h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="Serie" asp-action="DeletePost">
                    <input type="hidden" asp-for="SerieId" name="id"/>

                    <button type="submit" class="btn btn-danger fw-bold">Eliminar</button>
                    <a asp-controller="Serie" asp-action="Index" class="btn btn-warning fw-bold">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>
