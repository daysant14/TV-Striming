@model Application.ViewModels.SaveGeneroViewModel
@{
    ViewData["Title"] = "Eliminar generos";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>¿Está seguro que desea eliminar @Model.GeneroName del listado?</h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="Genero" asp-action="DeletePost">
                    <input type="hidden" asp-for="GeneroId" name="id" />

                    <button type="submit" class="btn btn-danger fw-bold">Eliminar</button>
                    <a asp-controller="Genero" asp-action="Index" class="btn btn-warning fw-bold">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>

